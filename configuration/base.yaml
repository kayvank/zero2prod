# config/base.yaml
# common settings.
# merge this with environment specific settings
#
application:
  port: 8000
database:
  host: "localhost"
  port: 5432
  username: "postgres"
  password: "postgres"
  database_name: "newsletter"
  require_ssl: false
